
i would like a plan for:
- building a streamlit app that display the network edges and the plots generated by function draw_lineage_plot()
- the streamlit app has to be build from scratch and
corresponding files should go in the folder streamlit_app
- i want the streamlit app to have three pages: a welcome page, 
a page with a tabular representation of the network edges using the streamlit-aggrid package
 and a third page for displaying the graphviz plot
- the page with the plot should only display the plot
 once filters on the network have been set. 
- functions related to filtering the network are in the file network_filter.py,
 i.e. primarily add_nodes_to_nx_filter() and get_filtered_network()
- the filtering options for the network should be made available by dropdown selections for:
  - dropdown selection for node attribute
  - dropdown selection for attribute values conditional on the selected node attribute
- once those two dropdown selections have been made by the user another button "add nodes to filtered network" should be available that calls add_nodes_to_nx_filter() based on those selections.
- there should be two more dropdown selections: one for descendant_level and one for ancestor_level, going from 1 to max path length in network (i added function max_path_length() in file network_filter that can be used for that)
- to generate the plot a button "generate plot" should be clicked that under the hood calls get_filtered_network() and passes the result to the draw_lineage_plot() function, which will generate the plot.

the following points should also be considered:
- if there are better output options for display in a webpage from graphviz than svg please advise about them
- the (unfiltered) network to display can be read from example_network.yaml for a start.

If anything is unclear or if there are central questions or decisions that should be included in the plan, do let me know
