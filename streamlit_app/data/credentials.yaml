# Allowlist for Data Lineage Explorer
#
# Each entry maps a SHA-256 hash of the OS login name to a display name.
# To generate a hash for a user, run:
#   python -c "import hashlib, getpass; print(hashlib.sha256(getpass.getuser().encode()).hexdigest())"
# Or for a specific username:
#   python -c "import hashlib; print(hashlib.sha256('jsmith'.encode()).hexdigest())"

users:
  76dcb8fe316e8294fe1dab594779460797194ef0052f6a06702c6e0fb2ce371b:  # gregorheyne
    display_name: Gregor Heyne
